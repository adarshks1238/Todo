import 'package:flutter/material.dart';

// Use 10.0.2.2 for Android Emulator, and localhost for iOS simulator or web.
// Change this to your local machine IP if running on physical device on the same Wifi.
const String baseUrl = 'http://localhost:5000/api';

class AppColors {
  static const Color primary = Color(0xFF6C63FF);
  static const Color secondary = Color(0xFFFF6584);
  static const Color background = Color(0xFFF9F9FB);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF2C2C2C);
  static const Color textSecondary = Color(0xFF8B8B8B);
  static const Color success = Color(0xFF4CAF50);
}
